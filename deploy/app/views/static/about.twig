{% set aside='company' %}
{% extends "content/custom_pages/custom_layout.twig" %}
{% block c_content %}
    <div class="about-block">
        <div class="about-block__img" style="background-image: url('/assets/images/static/about-us.jpg')">
        </div>
        <div>
            {{ fields.company_text|raw }}
        </div>
        <h2>В нашем интернет магазине Вы можете выбрать и заказать:</h2>
        <ul class="about-block__list">
            {% for item in global.menu_cats.levels[0] %}
                {% for sub_item in global.menu_cats.levels[item.cat_id] %}
                    <li><a href="{{ sub_item.cat_full_url }}">{{ sub_item.cat_short_title }}</a></li>
                {% endfor %}
            {% endfor %}
        </ul>
        <div class="dotted-divider"></div>
        <div>
            <h2>{{ fields.company_text2|raw }}</h2>
        </div>
       <div class="about-block__warranty-out">
           {% for item in fields.company_gar %}
               <div class="about-block__warranty">
                   <div class="about-block__warranty-nav">
                       <div>
                           <div class="about-block__warranty-icon">
                               {% include 'frontend/elements/svg/svg.twig' with {
                                   'name':item.gar_icon,
                                   'class':item.gar_icon,
                                   'embed':1
                               }%}
                           </div>
                           <div class="about-block__warranty-title">
                               {{ item.gar_title }}
                           </div>
                       </div>
                       <i class="fa fa-chevron-down" aria-hidden="true"></i>
                   </div>
                   <div class="about-block__warranty-text">
                       {{ item.gar_text }}
                   </div>
               </div>
           {% endfor %}
       </div>

        <div class="dotted-divider"></div>

        <div class="about-block__attention">
            <div class="about-block__attention-icon">
                {% include 'frontend/elements/svg/svg.twig' with {
                'name':'attention',
                'class':'attention',
                'embed':1,
                'width': 16,
                'height': 16
                }%}
            </div>
            <div>
                <span>Внимание.</span>
                <p>Весь товар строго сертифицирован согласно всех международных стандартов качества, на который
                    действует гарантийные обязательства.</p>
            </div>
        </div>
        <div>
            {{ fields.company_text3|raw }}
        </div>
        <div class="about-block__rekvizitu">
            <h2>Наши реквизиты</h2>
            {% include 'frontend/components/rekvizitu/rekvizitu.twig' %}
        </div>

    </div>
{% endblock %}
